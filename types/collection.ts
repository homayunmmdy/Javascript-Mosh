export interface CardsType {
  id: string;
  title: string;
  url?: string;
  imageurl?: string;
  tools: string[];
  live_demo: string;
  source_code: string;
}

export interface DSACardsType {
  id: string;
  title: string;
  url?: string;
  imageurl?: string;
  explanation: string;
  source_code: string;
}
